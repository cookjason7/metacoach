[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = ["npm install", "npm install --prefix server", "cd client && npm install"]

[phases.build]
cmds = ["cd client && npm run build"]

[start]
cmd = "node server/index.js"
