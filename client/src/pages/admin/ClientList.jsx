// ClientList.jsx — The Command Center has been merged into the unified Coaching Dashboard.
import { Navigate } from 'react-router-dom'

export default function ClientList() {
  return <Navigate to="/dashboard" replace />
}
