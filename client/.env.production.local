# Overrides .env.local for production builds so dist uses relative paths (Railway same-origin serving).
# .env.local has VITE_API_URL pointing to production — this file has higher Vite priority and clears it.
VITE_API_URL=
